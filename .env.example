# Metropy 환경 변수 설정 예시
# 실제 사용 시 .env 파일로 복사하여 사용하세요

# 서버 설정
HOST=127.0.0.1
PORT=8000
RELOAD=True

# 데이터 디렉토리
DATA_DIR=data_processed
RAW_DIR=data_raw

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# CORS 설정 (프로덕션 환경에서는 특정 도메인으로 제한)
ALLOWED_ORIGINS=*
